// file for declaring parameters of genetic algorithm

// size of each mumber (total genetic data is this number squared)
const GENE_SIZE = 10;

// total number of members in each generation
const POPULATION = 100;

// total number of generations
const GENERATIONS = 500;

// percentage of cells mutated when breeding
const MUTATION_RATE= 3;

// cycle will break when average fitness score reaches this level
const AVERAGE_FITNESS_SCORE_TARGET = 80;
